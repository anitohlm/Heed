node_modules/
.next/
dist/
__pycache__/
*.pyc
.venv/
local.settings.json
.env
.env.local
.azure/
*.log
.DS_Store
*.tmp
.claude/
# Word lock files — auto-created when a .docx is open, should never be tracked
~$*.docx

# Agent reports — contain security findings and test failures, never commit
docs/qa-reports/
docs/security-reports/

# Submission pack — local-only working doc for the demo recording
docs/SUBMISSION_PACK.md

# Solution architecture PDF — local-only artefact, generator stays in repo
docs/Heed-Solution-Architecture.pdf